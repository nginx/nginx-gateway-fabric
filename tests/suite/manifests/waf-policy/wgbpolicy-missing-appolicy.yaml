apiVersion: gateway.nginx.org/v1alpha1
kind: WAFGatewayBindingPolicy
metadata:
  name: gateway-waf-missing-appolicy
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: gateway
  apPolicySource:
    name: nonexistent-appolicy
  securityLogs:
  - apLogConfSource:
      name: logconf
    destination:
      type: stderr
