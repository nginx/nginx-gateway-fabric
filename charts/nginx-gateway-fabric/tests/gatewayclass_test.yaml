suite: test gatewayclass
templates:
  - gatewayclass.yaml
tests:
  - it: should render a gateway class with the correct name by default
    asserts:
      - isKind:
          of: GatewayClass
      - equal:
          path: metadata.name
          value: nginx

  - it: should render a gateway class with a custom name when set by a user
    set:
      nginxGateway.gatewayClassName: test
    asserts:
      - isKind:
          of: GatewayClass
      - equal:
          path: metadata.name
          value: test

  - it: should render the correct labels by default
    asserts:
      - isKind:
          of: GatewayClass
      - equal:
          path: metadata.labels
          value:
            helm.sh/chart: nginx-gateway-fabric-2.7.2
            app.kubernetes.io/name: nginx-gateway-fabric
            app.kubernetes.io/instance: RELEASE-NAME
            app.kubernetes.io/version: "edge"
            app.kubernetes.io/managed-by: Helm

  - it: should not render GatewayClass annotations by default
    asserts:
      - isKind:
          of: GatewayClass
      - notContains:
          path: metadata.annotations

  - it: should render the controllerName and parametersRef by default
    asserts:
      - isKind:
          of: GatewayClass
      - equal:
          path: spec.controllerName
          value: gateway.nginx.org/nginx-gateway-controller
      - equal:
          path: spec.parametersRef
          value:
            group: gateway.nginx.org
            kind: NginxProxy
            name: RELEASE-NAME-proxy-config
            namespace: NAMESPACE

  - it: should render the controllerName and parametersRef when set by the user
    set:
      nginxGateway.gatewayControllerName: gateway.nginx.org/test
    asserts:
      - isKind:
          of: GatewayClass
      - equal:
          path: spec.controllerName
          value: gateway.nginx.org/test
      - equal:
          path: spec.parametersRef
          value:
            group: gateway.nginx.org
            kind: NginxProxy
            name: RELEASE-NAME-proxy-config
            namespace: NAMESPACE
